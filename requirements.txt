# ============================================================
# AI for Scientific and Engineering — pip requirements
# Usage: pip install -r requirements.txt
#
# For GPU support install PyTorch from https://pytorch.org
# before running this file (select the correct CUDA version).
# ============================================================

# Core scientific computing
numpy>=1.26
scipy>=1.12
matplotlib>=3.8
pandas>=2.1

# PyTorch ecosystem (CPU wheels — override for GPU via pytorch.org)
torch>=2.2
torchvision>=0.17
torchaudio>=2.2

# Jupyter
jupyterlab>=4.1
ipywidgets>=8.1
notebook>=7.1

# ML utilities
scikit-learn>=1.4
tqdm>=4.66
tensorboard>=2.16
torchinfo>=1.8

# Generative models
diffusers>=0.27
einops>=0.8

# Reinforcement learning
gymnasium>=0.29
stable-baselines3>=2.3

# Visualization
seaborn>=0.13
plotly>=5.20

# Optional JAX track (Lê Nguyễn Ngọc Vũ)
# CPU-only wheels below. For GPU:
#   pip install -U "jax[cuda12]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
jax>=0.4.25
jaxlib>=0.4.25
flax>=0.8
optax>=0.2
orbax-checkpoint>=0.5
